name: ci

on:

  pull_request:
    branches:
      - master
      - main
      - cpp17
    paths-ignore:
      - '**/*.md'

jobs:

  linux:

    name: "Linux"
    runs-on: ubuntu-22.04
    steps:
    - name: Clone repo
      uses: actions/checkout@v3
    - name: Run CMake
      run: mkdir build && ls -la && cmake ../
    - name: Build
      run: make -j`nproc`
      working-directory: build

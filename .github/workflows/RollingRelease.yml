name: RollingRelease

on:
  push:
    branches: [master]
    paths-ignore:
      - "**/README.md"
  pull_request:
    branches: [master]
    paths-ignore:
      - "**/README.md"

jobs:
  drill-linux-cli:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - run: cd Docker && docker-compose up --build drill-linux-cli

  drill-linux-gtk:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - run: cd Docker && docker-compose up --build drill-linux-gtk

  drill-windows-cli:
    runs-on: windows-2019
    steps:
      - uses: actions/checkout@v2
      - run: cd Docker && docker-compose up --build drill-windows-cli
        shell: cmd
